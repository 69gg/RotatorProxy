# RotatorProxy example configuration.
# Copy this file to config.toml and adjust values for your environment.

# The single local proxy port. It auto-detects HTTP/HTTPS CONNECT and SOCKS5.
listen = "127.0.0.1:7890"

# Files or directories to scan for outbound proxies.
# Plain proxy lines can include HTTP, HTTPS, SOCKS4/5, Shadowsocks, and common
# Clash-compatible URI formats.
# Directories are scanned non-recursively and files are processed in sorted order.
proxy_dirs = ["./proxies"]

# Maximum outbound proxies to try for one inbound request when connection setup fails.
# Set to 0 to try every currently available proxy once. If no proxies are
# loaded, RotatorProxy connects directly.
max_retries = 10

# Timeout for connecting to a target or outbound proxy.
connect_timeout_ms = 10000

# Maximum number of client connections handled concurrently. The listener
# applies backpressure before accept() so inbound traffic cannot consume an
# unbounded number of file descriptors.
max_concurrent_connections = 256

# Timeout for fetching subscription URLs found in proxy source files.
subscription_timeout_ms = 15000

# User-Agent sent when fetching subscription URLs.
subscription_user_agent = "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"

# Optional proxy used when fetching subscription/Clash config URLs and when
# auto-downloading Mihomo. This does not affect runtime outbound rotation.
# Supported schemes: http, https, socks4, socks4a, socks5, socks5h.
# Use socks5h when you want DNS resolution to happen through the proxy.
# subscription_proxy = "socks5h://127.0.0.1:1080"

# Default log level. RUST_LOG overrides this value.
log_level = "info"

# Enable startup/refresh health prechecks. Set this to false to load every
# candidate node into the active pool immediately.
health_check_enabled = true

# HTTP or HTTPS endpoint used to test outbound proxy liveness.
# The default mirrors Clash/Mihomo-style URL delay checks.
health_check_url = "http://cp.cloudflare.com/generate_204"

# Expected HTTP status for a successful health check. Supports exact codes and
# comma-separated ranges. Use "204" for strict generate_204 checks.
health_check_expected_status = "200-399"

# A node must pass within this many attempts to enter the active rotation pool.
health_check_attempts = 3

# Timeout for each health-check attempt.
health_check_timeout_ms = 10000

# Maximum number of proxy nodes checked at the same time.
# Large public proxy lists usually need 128-512 to finish background refreshes
# in a reasonable time.
health_check_concurrency = 256

# Maximum number of disabled nodes silently rechecked at the same time during
# background startup/refresh. Keep this lower than the main precheck concurrency
# when the process has many disabled complex nodes or a small file-descriptor limit.
disabled_recheck_concurrency = 128

# When health_check_url uses https://, keep certificate verification enabled by
# default. Set this to true only for a private/self-signed health endpoint.
health_check_tls_skip_verify = false

# Runtime connection failures before an active node enters cooldown.
runtime_failure_threshold = 3

# Cooldown entries in one refresh cycle before a node is disabled until the
# next successful source/subscription refresh.
runtime_disable_after_cooldowns = 2

# How long a runtime-failing node is skipped before it can be tried again.
cooldown_seconds = 300

# Persist the active proxy pool and failure state across restarts. The active
# pool cache stores full proxy configs and may include proxy credentials.
pool_state_enabled = true

# JSON file used for persisted active/disabled/retired proxy state.
pool_state_path = ".rotator-proxy/pool-state.json"

# Local time for the daily full reload + health check.
# Used only when refresh_interval_seconds is unset.
daily_refresh_time = "04:00"

# Optional fixed source/subscription refresh interval in seconds. When set, it
# takes precedence over daily_refresh_time.
# refresh_interval_seconds = 3600

# Historical compatibility option. Mihomo fallback is currently disabled in
# the refresh path; complex nodes that the in-process backend cannot dial are
# skipped instead of being handed to a Mihomo sidecar.
mihomo_enabled = false

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
# mihomo_binary = "/usr/local/bin/mihomo"

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_auto_download = false

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_work_dir = ".rotator-proxy/mihomo"

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_log_level = "warning"

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_generation_batch_size = 256

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_startup_timeout_ms = 10000

# Historical Mihomo fallback setting; currently unused while fallback is
# disabled.
mihomo_retire_grace_seconds = 300
