/target/

# Local runtime configuration. Copy config.toml.example to config.toml and edit it.
config.toml

# Local proxy source files and subscriptions.
proxies/

# Runtime sidecar binaries, generated Mihomo configs, and temporary state.
.rotator-proxy/
